FreeBSD 14 : Загрузка2023/12/13
	
[1]   Вы можете загрузить FreeBSD 14 (выпущена 20 ноября 2023 г.) со следующего сайта.

⇒ https://download.freebsd.org/releases/ISO-IMAGES/14.0/

Создайте установочный носитель для FreeBSD (USB-накопитель)
 	
Запишите загруженный файл образа на USB-накопитель, чтобы создать установочный носитель для FreeBSD.
Если вы создаете его в Windows, обратитесь к следующему.

	
[2]  Скачайте Win32 Disk Imager со следующего сайта.
⇒ https://sourceforge.net/projects/win32diskimager/files/Archive/

[3]	Подключите USB-накопитель к компьютеру.
Затем извлеките загруженный файл и запустите Win32 Disk Imager. Затем появится следующий экран.
Например, для компьютера x86 загрузите [FreeBSD-14.0-RELEASE-amd64-memstick.img] с сайта в разделе [1], а затем выберите файл образа и USB-накопитель на экране следующим образом. А затем нажмите кнопку [Записать], чтобы записать образ FreeBSD на USB-накопитель.

[4]	После завершения записи будет создана папка EFI следующим образом и создание USB-накопителя для установки будет завершено.

---

Перейдите в каталог, куда вы скачали образ FreeBSD... .img и выдайте команду:

# dd if=FreeBSD-12.1-RELEASE-amd64-memstick.img of=/dev/sdb bs=1m

где /dev/sdb - это устройство под которым у вас система распознала флэшноситель.

---

Вывести описание установленной ОС:

uname -a

Вывести список каталогов корневого раздела:

ls -l /

Список смонтированных файловых систем, с указанием типа файловых систем:

df -hT

Список собственно файловых систем zfs:

zfs list -v

Посмотреть состояние пула zfs:

zpool status -v

Сетевые настройки:

ifconfig

Загруженные модули ядра:

kldstat

---

1. xorg.
# pkg install xorg

2. video-driver
VMWare:
# pkg install xf86-video-vmware

Intel:
# pkg install xf86-video-intel

и установить mesa-dri - для ускорения графики.

# pkg install mesa-dri mesa-gallium-va mesa-gallium-vdpau mesa-gallium-xa mesa-libs

В принципе всё, что нужно для графического режима у нас есть.

Надо установить ещё менеджер графических сессий и саму графическую оболочку с которой мы будем работать. Менеджер графических сессий - это то, что даст вам интерфейс ввода имени пользователя и пароля:

# pkg install sddm

И оболочку, коих великое множество:

gnome, kde, xfce, lxfce, openbox и т.п

Устанавливаем:

# pkg install sddm

# pkg install kde5

после окончания установки будет выдан очень обширный список сообщений и ценных сведений... который в общем надо бы почитать.

Но их там оооочень много, потому самое важное я вынес ниже:

Добавляем в /etc/rc.conf:

webcamd_enable="YES"
smartd_enable="YES"

И добавляем вашего пользователя в группу использующих веб-камеру:

# pw groupmod webcamd -m <username>

Так как у нас это графическая рабочая станция, то прописываем в /etc/rc.conf старт менеджера графических сессий:

sddm_enable="YES"

Добавить в /etc/sysctl.conf:

sysctl net.local.stream.recvspace=65536
sysctl net.local.stream.sendspace=65536
kern.ipc.shmmax=67108864
kern.ipc.shmall=32768

=====

Добавить в /boot/loader.conf:

cuse_load="YES"

Теперь можно перезагрузиться...

Если всё прошло нормально, то в результате загрузки вы попадёте в графический менеджер сессий sddm

У фряхи сейчас своё рабочее окружение есть. Lumina

Поместить в ~./fluxbox/startup
setxkbmap -layout "us,ru" -option "grp:ctrl_shift_toggle,grp_led:scroll"



